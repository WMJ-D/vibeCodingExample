-- =========================================================
-- 文件系统菜单归属主系统
-- 说明：文件系统当前属于 main 子系统，避免其他子系统看到该菜单。
-- =========================================================

UPDATE `sys_menu`
SET `app_id` = 'main'
WHERE `id` IN (411, 4111, 4112, 4113)
  AND `deleted` = 0;
